- hosts: localhost
  tasks:  
  - name: Troca imagem de fundo e posiciona icones padrões do Desktop
    copy: src=files/desktop-items-0.conf dest=/home/pi/.config/pcmanfm/FXDE-pi
    sudo: yes
  - name: Personaliza barra superior
    copy: src=files/panel dest=/home/pi/.config/lxpanel/LXDE-pi/panel
    sudo: yes
